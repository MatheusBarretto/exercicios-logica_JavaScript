// Faça um Programa que converta metros para centímetros.

alert(`Convertendo metros em centímetros`)

let metragrem = Number(prompt('Digite a quantidade (em metros): '))

alert(`${metragrem} metros é o mesmo que ${metragrem*100} centímetros`)